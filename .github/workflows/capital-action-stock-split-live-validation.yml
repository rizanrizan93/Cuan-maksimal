name: Phase 5.6 capital action stock-split live consumer - Emir

on:
  workflow_dispatch:
    inputs:
      selected_ref:
        description: Exact commit SHA to validate
        required: true
        type: string
      year:
        description: Source year (YYYY)
        required: true
        default: "2026"
        type: string
      month:
        description: Source month (1-12)
        required: true
        default: "7"
        type: string
      observed_on:
        description: Evidence observation date (YYYY-MM-DD)
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: phase5-6-capital-action-stock-split-live-validation
  cancel-in-progress: false

env:
  PYTHONPATH: ${{ github.workspace }}
  PYTHONDONTWRITEBYTECODE: "1"

jobs:
  stock-split-consumer:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      SHARED_EVIDENCE_SUPABASE_URL: ${{ secrets.SHARED_EVIDENCE_SUPABASE_URL }}
      SHARED_EVIDENCE_SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SHARED_EVIDENCE_SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - name: Checkout exact selected revision
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.selected_ref }}
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install bounded stock-split validator dependencies
        run: python -m pip install --disable-pip-version-check --no-cache-dir requests==2.34.2

      - name: Run bounded stock-splits consumer proof
        run: |
          mkdir -p capital_action_stock_split_output
          set -o pipefail
          python tools/phase5_6_stock_split_live_validation.py \
            --mode consumer \
            --client-id EMIR \
            --feed stock-splits \
            --year "${{ inputs.year }}" \
            --month "${{ inputs.month }}" \
            --observed-on "${{ inputs.observed_on }}" \
            | tee capital_action_stock_split_output/summary.log

      - name: Record selected revision
        if: always()
        run: |
          mkdir -p capital_action_stock_split_output
          git rev-parse HEAD > capital_action_stock_split_output/selected-commit-sha.txt

      - name: Upload bounded proof
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: phase5-6-capital-action-stock-split-consumer-proof
          path: capital_action_stock_split_output/
          if-no-files-found: error
