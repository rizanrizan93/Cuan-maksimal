name: Phase 5.6 announcement live consumer - Emir

on:
  workflow_dispatch:
    inputs:
      selected_ref:
        description: Exact commit SHA to validate
        required: true
        type: string
      feed:
        description: Shared metadata feed already produced by Pasticuan
        required: true
        default: announcements
        type: choice
        options:
          - announcements
          - press-release
      publication_date:
        description: Exact cached publication date (YYYY-MM-DD)
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: phase5-6-announcement-live-validation
  cancel-in-progress: false

env:
  PYTHONPATH: ${{ github.workspace }}
  PYTHONDONTWRITEBYTECODE: "1"

jobs:
  announcement-consumer:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    env:
      SHARED_EVIDENCE_SUPABASE_URL: ${{ secrets.SHARED_EVIDENCE_SUPABASE_URL }}
      SHARED_EVIDENCE_SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SHARED_EVIDENCE_SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - name: Checkout exact selected revision
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.selected_ref }}
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install cache-consumer dependencies
        run: python -m pip install --disable-pip-version-check --no-cache-dir requests==2.34.2

      - name: Run cache-only announcement consumer proof
        run: |
          mkdir -p announcement_live_output
          set -o pipefail
          python tools/phase5_6_announcement_live_validation.py \
            --mode consumer \
            --client-id EMIR \
            --feed "${{ inputs.feed }}" \
            --publication-date "${{ inputs.publication_date }}" \
            | tee announcement_live_output/summary.log

      - name: Record selected revision
        if: always()
        run: |
          mkdir -p announcement_live_output
          git rev-parse HEAD > announcement_live_output/selected-commit-sha.txt

      - name: Upload cache-reuse proof
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: phase5-6-announcement-consumer-proof
          path: announcement_live_output/
          if-no-files-found: error
